<div class="navbar navbar-default navbar-fixed-top" role="navigation">
    <div class="container main-menu">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li>
                    <a href="/"><i class="glyphicon glyphicon-home"></i></a>
                </li>
                <li>
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Danh Mục<b
                                class="caret"></b></a>
                    <ul class="dropdown-menu">
                        {% for category in categories %}
                            {% if category['children'] == false %}
                                <li>
                                    <a href="{{ path('app_category',{'alias':category.alias}) }}">{{ category.name }}</a>
                                </li>
                            {% else %}
                                <li><a href="{{ path('app_category',{'alias':category.alias}) }}"
                                       class="dropdown-toggle" data-toggle="dropdown">{{ category.name }}
                                        <b class="caret"></b></a>
                                    <ul class="dropdown-menu">
                                        {% for cc in category['children'] %}
                                            <li>
                                                <a href="{{ path('app_category',{'alias':cc.alias}) }}">{{ cc.name }}</a>
                                            </li>
                                            {% if cc['children'] == false %}
                                            {% else %}
                                                <li>
                                                    <a href="{{ path('app_category',{'alias':cc.alias}) }}"
                                                       class="dropdown-toggle" data-toggle="dropdown">{{ cc.name }}
                                                        <b class="caret"></b>
                                                    </a>
                                                    <ul class="dropdown-menu">
                                                        {% for ccc in cc['children'] %}
                                                            <li>
                                                                <a href="{{ path('app_category',{'alias':ccc.alias}) }}">{{ ccc.name }}</a>
                                                            </li>
                                                            {% if ccc['children'] == false %}
                                                            {% else %}
                                                                <li>
                                                                    <a href="{{ path('app_category',{'alias':ccc.alias}) }}">{{ ccc.name }}</a>
                                                                </li>
                                                            {% endif %}
                                                        {% endfor %}
                                                    </ul>
                                                </li>
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                </li>
                            {% endif %}
                        {% endfor %}
                    </ul>
                </li>
                <li>
                    <a href="javascript:;" class="dropdown-toggle"
                       data-toggle="dropdown">Thương Hiệu<b
                                class="caret"></b></a>
                    <ul class="dropdown-menu">
                        {% for td in trademark %}
                            <li>
                                <a href="{{ path('app_category',{'alias':td.alias}) }}"
                                   title="{{ td.name }}">{{ td.name }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ path('app_list_new') }}">Cẩm Nang Làm Đẹp</a></li>
                <li><a href="{{ path('app_category',{'alias':'san-pham-ban-chay'}) }}">Sản Phẩm Bán Chạy</a></li>
                <li><a href="{{ path('app_category',{'alias':'san-pham-khuyen-mai'}) }}">Sản Phẩm Khuyến Mãi</a></li>
                <li><a href="{{ path('app_contact') }}">Liên Hệ</a></li>
            </ul>
            <ul class="nav navbar-nav pull-right">
                <li class="">
                    <a href="{{ path('app_product_card') }}" title="Giỏ hàng">
                        <i class="fa fa-cart-arrow-down" aria-hidden="true"></i> ({{ card }})
                    </a>
                </li>
                <li>
                    <a href="javascript:;">Hotline : 0971.319.345
                    </a>
                </li>
            </ul>
        </div>
        <!--/.nav-collapse -->
    </div>
</div>
