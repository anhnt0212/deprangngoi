<div class="navbar navbar-default navbar-fixed-top sb-slide" role="navigation" style="position:fixed">
    <div class="container main-menu">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li>
                    <a href="/"><i class="glyphicon glyphicon-home"></i></a>
                </li>
                <li class="dropdown"><a href="javascript:;" title="Danh mục" class="dropdown-toggle"
                                        data-toggle="dropdown">Danh Mục<span class="caret"></span></a>
                    <ul class="dropdown-menu" role="menu">
                        {% for category in categories %}
                            {% if category['children'] == false %}
                                <li><a href="{{ path('app_category',{'alias':category.alias}) }}"
                                       title="{{ category.name }}">{{ category.name }}</a></li>
                            {% else %}
                                <li class="dropdown-submenu">
                                    <a href="{{ path('app_category',{'alias':category.alias}) }}" tabindex="-1"
                                       class="dropdown-toggle disabled" onclick="window.location.href='{{ path('app_category',{'alias':category.alias}) }}'" data-toggle="dropdown">{{ category.name }}</a>
                                    <ul class="dropdown-menu">
                                        {% for cc in category['children'] %}
                                            {% if cc['children'] == false %}
                                                <li>
                                                    <a href="{{ path('app_category',{'alias':cc.alias}) }}"
                                                       title="{{ cc.name }}">{{ cc.name }}</a>
                                                </li>
                                            {% else %}
                                                <li class="dropdown-submenu">
                                                    <a href="{{ path('app_category',{'alias':cc.alias}) }}"
                                                       onclick="window.location.href='{{ path('app_category',{'alias':cc.alias}) }}'" tabindex="-1" class="dropdown-toggle"
                                                       data-toggle="dropdown">{{ cc.name }}</a>
                                                    <ul class="dropdown-menu">
                                                        {% for ccc in cc['children'] %}
                                                            <li><a href="{{ path('app_category',{'alias':ccc.alias}) }}"
                                                                   title="{{ ccc.name }}">{{ ccc.name }}</a></li>
                                                        {% endfor %}
                                                    </ul>
                                                </li><!--end submenu-->
                                            {% endif %}
                                        {% endfor %}
                                    </ul>
                                </li><!--end submenu-->
                            {% endif %}
                        {% endfor %}
                    </ul>
                </li>
                <li>
                    <a href="javascript:;" class="dropdown-toggle"
                       data-toggle="dropdown">Thương Hiệu<b
                                class="caret"></b></a>
                    <ul class="dropdown-menu">
                        {% for td in trademark %}
                            <li>
                                <a href="{{ path('app_category',{'alias':td.alias}) }}"
                                   title="{{ td.name }}">{{ td.name }}</a>
                            </li>
                        {% endfor %}
                    </ul>
                </li>
                <li><a href="{{ path('app_category',{'alias':'san-pham-ban-chay'}) }}">SP Bán Chạy</a></li>
                <li><a href="{{ path('app_category',{'alias':'san-pham-khuyen-mai'}) }}">SP Khuyến Mãi</a></li>
                <li><a href="{{ path('app_list_new') }}">Cẩm Nang Làm Đẹp</a></li>
                <li><a href="{{ path('app_contact') }}">Liên Hệ</a></li>
            </ul>
            <ul class="nav navbar-nav pull-right">
                <li class="">
                    <a href="{{ path('app_product_card') }}" title="Giỏ hàng">
                        <i class="fa fa-cart-arrow-down" aria-hidden="true"></i> ({{ card }})
                    </a>
                </li>
                <li>
                    <a href="javascript:;">Hotline : 0938.367.123
                    </a>
                </li>
            </ul>
        </div>
    </div>
</div>